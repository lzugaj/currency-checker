export enum ButtonSignType {
  PLUS = "+",
  MINUS = "-",
}

export enum OtherSignType {
  DOLLAR = "$",
}

export enum CurrencyType {
  AUD = "AUD",
}

export enum CurrencyValueType {
  EXCHANGE_RATE_NUMBER = "Broj tečajnice",
  DATE_OF_APPLICATION = "Datum primjene",
  COUNTRY = "Država",
  UNIT = "Jedinica",
  PURCHASE_FOR_FOREIGN_EXCHANGE = "Kupovni za devize",
  MEDIUM_FOR_FOREIGN_EXCHANGE = "Srednji za devize",
  FOREIGN_EXCHANGE_SALES = "Prodajni za devize",
  CURRENCY = "Valuta",
  CURRENCY_CODE = "Šifra valute",
}

export enum DisplayType {
  HIDDEN = "hidden",
  FLEX = "flex",
}

export enum VisibilityType {
  VISIBLE = "visible",
  INVISIBLE = "invisible",
}
